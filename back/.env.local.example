# =============================================================================
# Configuration Supabase Local (Dev)
# =============================================================================
# Copier ce fichier vers .env.local et remplir les valeurs
# Les credentials Supabase sont affichés par `supabase start`

# Supabase self-hosted (Docker)
supabase_url="http://127.0.0.1:54321"
supabase_secret="<service_role_key_from_supabase_start>"

# GoCardless API (mêmes credentials qu'en prod ou mock)
GO_CARDLESS_SECRET_ID="your-gocardless-id"
GO_CARDLESS_SECRET_KEY="your-gocardless-key"

# Clé de chiffrement AES-256 (64 caractères hex)
# Générer avec: python -c "import secrets; print(secrets.token_hex(32))"
ENCRYPTION_KEY="your-64-char-hex-key"

# API Port (doit correspondre à NEXT_PUBLIC_API_URL du frontend)
API_PORT=8080
